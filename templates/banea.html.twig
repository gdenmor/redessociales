<html>
<html>
    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <title>Búsqueda de Usuarios</title>
        <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.0/dist/css/bootstrap.min.css" rel="stylesheet">
        <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/jqueryui/1.12.1/jquery-ui.min.css">
        <script src="https://cdnjs.cloudflare.com/ajax/libs/jquery/3.5.1/jquery.min.js"></script>
        <script src="https://cdnjs.cloudflare.com/ajax/libs/jqueryui/1.12.1/jquery-ui.min.js"></script>
        <script src="https://cdnjs.cloudflare.com/ajax/libs/popper.js/1.16.0/umd/popper.min.js"></script>
        <script src="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/js/bootstrap.min.js"></script>
        <link href="{{asset('css/admin.css')}}" rel="stylesheet">
        <style>
        th {
            text-align: center;
            padding-left: 2em;
        }
        td{
            text-align: center;
        }
        .glyphicon {
            margin-right: 10px;
        }
        @media (max-width: 500px) {
            th {
                text-align: center;
                padding: 0.5em;
            }
    
        }
        .crud-table {
            width: 100%;
            padding: 40px 0;
        }
        .form-alert {
            margin-top: 10px;
        }
        .form-inline {
            margin-bottom: 20px;
        }
        #buscar{
            background-color: #E1E1E1;
        }
        
    </style>
    </head>
<body>
    {% include "headeradmin.html.twig" %}
    <div id="content-wrapper">
    <div class="container">
        <div>
            {% if mensajes %}
                <h1 style="margin-bottom: 10%;">BANEAR USUARIOS</h1>
                <table style="margin-top: 15%;">
                    <thead>
                        <tr>
                            <th>Usuario reportado</th>
                            <th>Usuario que reporta</th>
                            <th>Mensaje</th>
                            <th>Acción</th>
                        </tr>
                    </thead>
                    <tbody>
                        {% for mensaje in mensajes %}
                            <tr>
                                <td style="text-align:center;">{{mensaje.receptor.usuario}}</td>
                                <td style="text-align:center;">{{mensaje.emisor.usuario}}</td>
                                <td style="text-align:center;">{{mensaje.mensaje}}</td>
                                <td style="text-align:center;"><a href="{{path('baneaid',{id: mensaje.receptor.id})}}"><button class="btn btn-danger">Banear Usuario</button></a></td>
                            </tr>
                        {% endfor %}
                    </tbody>
                </table>
            {% else %}
                <p style="color:red;margin-top: 20%;text-align:center;">No existen resultados disponibles</p>
            {% endif %}
        </div>
    </div>
</body>
</html>